<odoo>
    <record id="view_purchase_order_form_xprinter" model="ir.ui.view">
        <field name="name">purchase.order.form.xprinter</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- Ajouter le bouton dans la barre d’actions -->
            <xpath expr="//header" position="inside">
                <button name="action_print_xprinter"
                        string="Print XPrinter"
                        type="object"
                        class="btn-primary"
                        icon="fa-print"/>
            </xpath>

        </field>
    </record>
</odoo>
